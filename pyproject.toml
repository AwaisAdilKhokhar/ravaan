[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ravaan"
version = "0.1.0"
description = "Does masked diffusion pay off for a genuinely low-resource language? A matched AR/diffusion comparison for Urdu."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Muhammad Awais Bin Adil" }]
keywords = ["urdu", "language-model", "masked-diffusion", "low-resource", "nlp"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Stage 4 of the corpus pipeline needs nothing but the standard library, on
# purpose: normalization is the one component every later stage depends on, and
# it should stay auditable and dependency-free.
dependencies = []

[project.scripts]
ravaan-normalize = "ravaan.data.normalization:main"

[project.optional-dependencies]
data = [
    "datasets>=3.0",
    "huggingface-hub>=0.26",
    "pyarrow>=17",
    "zstandard>=0.23",
    "tqdm>=4.66",
]
tokenizer = ["sentencepiece>=0.2.0"]
train = ["torch>=2.4", "numpy>=1.26"]
dev = ["pytest>=8.0", "ruff>=0.6"]

[tool.setuptools.packages.find]
include = ["ravaan*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
